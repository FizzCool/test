集合遍历方式
1.通过数组
  先向上转型成对象数组，遍历时再向下转型成对应的类型来遍历集合
2.集合配备了御用遍历工具-->Iterator(迭代器)
   迭代器是依赖集合而存在的
  方法：
        boolean next()      如果有下一个元素则返回true
        E next()            获取并返回元素，移动到下一个位置
  使用方法: Iterator i = Collection.iterator();
